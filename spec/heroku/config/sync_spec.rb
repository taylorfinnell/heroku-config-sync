RSpec.describe Heroku::Config::Sync do
  it "has a version number" do
    expect(Heroku::Config::Sync::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
